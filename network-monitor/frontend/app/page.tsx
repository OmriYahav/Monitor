import {api} from '../components/api'; import StatusBadge from '../components/StatusBadge';
export default async function Dashboard(){const s=await api('/dashboard/summary');return <><h1>Dashboard</h1><div className="grid">{[['Total',s.total_devices],['Online',s.online_devices],['Offline',s.offline_devices],['Unknown',s.unknown_devices],['Open alerts',s.open_alerts_count]].map(x=><div className="card" key={x[0]}><h3>{x[0]}</h3><b>{x[1]}</b></div>)}</div><div className="card"><h2>Offline devices</h2>{s.offline_devices_list.map((d:any)=><p key={d.id}>{d.name} {d.ip_address} <StatusBadge status={d.status}/></p>)}</div><div className="card"><h2>Recent alerts</h2>{s.recent_alerts.map((a:any)=><p key={a.id}>{a.severity}: {a.title} - {a.status}</p>)}</div></>}
